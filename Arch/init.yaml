blueprints:
  format: yaml
  location: "."
  git:
    url: "https://github.com/thefynx/rwr-blueprints.git"
    target: "{{ .User.home }}/git/thefynx/rwr-blueprints"
packageManagers:
  - name: paru
    action: install
