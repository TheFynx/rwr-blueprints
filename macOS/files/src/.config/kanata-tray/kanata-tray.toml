# For help with configuration see https://github.com/rszyma/kanata-tray/blob/main/README.md#configuration
"$schema" = "https://raw.githubusercontent.com/rszyma/kanata-tray/main/doc/config_schema.json"

[general]
allow_concurrent_presets = false
control_server_enable = false
control_server_port = 8100

[defaults]
tcp_port = 5829
# Retry kanata if it dies (e.g. VHID daemon not ready yet at login). Note:
# kanata-tray disables this after >2 restarts/min, so the wait loop in
# kanata-sudo.sh is the primary boot-race fix; this is the backstop.
autorestart_on_crash = true

[defaults.hooks]
# Hooks allow running custom commands on specific events (e.g. when starting preset).
# Documentation: https://github.com/rszyma/kanata-tray/blob/main/doc/hooks.md

[defaults.layer_icons]


[presets.'Default Preset']
kanata_executable = '{{ .User.home }}/.config/kanata/kanata-sudo.sh'
kanata_config = '{{ .User.home }}/.config/kanata/kanata.kbd'
autorun = true

[presets.'External Keyboard']
kanata_executable = '{{ .User.home }}/.config/kanata/kanata-sudo.sh'
kanata_config = '{{ .User.home }}/.config/kanata/kanata-external.kbd'
autorun = false
