packages:
  # Import common base packages shared with Arch
  # See: Common/packages/arch/base-pacman.yaml
  - import: ../../Common/packages/arch/base-pacman.yaml
  # Archcraft-specific packages not in base
  - names:
      - p7zip
      - brave-browser
    action: install
    package_manager: pacman
