packages:
  # Import common base AUR packages shared with Arch
  # See: Common/packages/arch/base-aur.yaml
  - import: ../../Common/packages/arch/base-aur.yaml
  # Archcraft-specific AUR packages
  - names:
      - nvm
      - hoard
    action: install
    package_manager: paru
